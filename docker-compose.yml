services:
  rag:
    build: .
    command: uvicorn apps.rag_qa.api:app --host 0.0.0.0 --port 8000 --reload
    ports: ["8000:8000"]
  recsys:
    build: .
    command: uvicorn apps.recsys_api.api:app --host 0.0.0.0 --port 8001 --reload
    ports: ["8001:8001"]
